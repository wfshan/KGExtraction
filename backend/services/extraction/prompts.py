"""
Prompt 模板集中管理
所有抽取相关的 Prompt 模板
"""

# ============================================================
# 实体抽取 Prompt
# ============================================================

ENTITY_EXTRACTION_SYSTEM = """你是一个专业的知识图谱实体抽取专家。你的任务是从给定文本中严格按照提供的 Schema 抽取实体。

## 核心准则
1. **严格对齐**：只抽取 Schema 定义中存在的实体类型。**严禁**抽取任何未定义的类型。
2. **宁缺毋滥**：如果不确定某个实体属于哪个定义好的类型，请不要抽取。
3. **标准化**：实体名称应当简洁、标准化。
4. **证据锚定**：每个实体必须给出 `evidence` 字段，填入原文中能直接支撑该实体的短句（逐字摘录）。

## Schema 定义（仅允许抽取以下类型）
{entity_types_desc}

## 输出要求
请严格按照 JSON 格式输出，包含一个 entities 数组。如果无匹配项，返回空数组。
{{
  "entities": [
    {{
      "name": "实体名称",
      "entity_type": "实体类型（必须在 Schema 中）",
      "properties": {{}},
      "evidence": "支撑该实体的原文短句（逐字摘录）",
      "confidence": 0.95
    }}
  ]
}}
"""

ENTITY_EXTRACTION_USER = """请从以下文本中抽取实体：

{text}
"""

# ============================================================
# 实体消歧 Prompt
# ============================================================

ENTITY_DISAMBIGUATION_SYSTEM = """你是一个实体消歧专家。你需要判断新抽取的实体是否与已有实体库中的某个实体是同一个实体。

## 判断标准
1. 名称完全相同或高度相似（别名、缩写等）
2. 类型相同
3. 上下文语义一致

## 输出要求
对于每个新实体，请判断其是否与候选列表中的某个实体相同。请严格按照 JSON 格式输出：
{{
  "decisions": [
    {{
      "new_entity_name": "新实体名称",
      "match_entity_id": "匹配的已有实体ID（如果是新实体则为 null）",
      "is_same": true/false,
      "reason": "判断理由"
    }}
  ]
}}
"""

ENTITY_DISAMBIGUATION_USER = """## 新抽取的实体
{new_entities}

## 候选已有实体（向量召回）
{candidate_entities}

## 当前文本上下文
{context}

请判断新实体是否与候选中的某个实体为同一实体。
"""

# ============================================================
# 关系推断 Prompt
# ============================================================

RELATION_EXTRACTION_SYSTEM = """你是一个知识图谱关系抽取专家。你的任务是根据 Schema 定义，推断给定实体之间的关系。

## 核心准则
1. **严格类型对齐**：关系类型必须完全匹配 Schema 定义。
2. **严格路径对齐**：**严禁**建立 Schema 定义以外的实体类型组合关系。例如 Schema 定义 A -> 包含 -> B，则严禁抽取 A -> 包含 -> A（除非 Schema 明确允许）。
3. **依据文本**：只抽取在文本中有明确证据支持的关系。
4. **证据锚定**：每条关系必须给出 `evidence` 字段，填入原文中能直接支撑该关系的短句（逐字摘录）。

## Schema 关系定义
{relation_types_desc}

## 待分析实体列表
{entities_in_context}

## 输出要求
请严格按照 JSON 格式输出。如果无匹配项，返回空数组。
{{
  "relations": [
    {{
      "source_name": "源实体名称（必须来自待分析列表）",
      "target_name": "目标实体名称（必须来自待分析列表）",
      "relation_type": "关系类型（必须在 Schema 中且符合类型约束）",
      "properties": {{}},
      "evidence": "支撑该关系的原文短句（逐字摘录）",
      "confidence": 0.9
    }}
  ]
}}
"""

RELATION_EXTRACTION_USER = """请从以下文本中抽取实体间的关系：

{text}
"""

# ============================================================
# 跨片段关系推断 Prompt
# ============================================================

CROSS_CHUNK_RELATION_SYSTEM = """你是一个知识图谱跨片段推理专家。根据 Schema（领域本体）中的关系类型定义，推断当前文本中的实体与全局图谱中其他实体之间可能存在的关系；推断结果将参与图谱构建与多跳溯源。

## Schema 关系定义
{relation_types_desc}

## 当前文本中的实体
{current_entities}

## 全局图谱中的候选关联实体
{global_entities}

## 输出要求
请严格以 JSON 格式输出：
{{
  "relations": [
    {{
      "source_name": "源实体名称",
      "target_name": "目标实体名称",
      "relation_type": "关系类型",
      "confidence": 0.8,
      "reason": "推断依据"
    }}
  ]
}}

## 注意事项
1. 只推断有合理依据的跨片段关系
2. confidence 低于 0.7 的关系请不要输出
"""

CROSS_CHUNK_RELATION_USER = """当前文本片段：
{text}

请推断跨片段的实体关系。
"""

# ============================================================
# 自我修正 Prompt
# ============================================================

SELF_CORRECTION_SYSTEM = """你是一个知识图谱质量审核专家。请检查以下抽取结果是否有逻辑错误并修正，使结果符合领域本体（Schema）约束，以支持后续图谱多跳推理与溯源。

## 检查项
1. 实体类型是否与 Schema 一致且合理
2. 关系的源/目标实体类型是否匹配 Schema 约束
3. 是否有明显的重复或矛盾
4. 置信度是否合理

## Schema 定义
{schema_desc}

## 输出要求
请严格以 JSON 格式输出：
{{
  "corrections": [
    {{
      "type": "entity" 或 "relation",
      "action": "remove" 或 "modify",
      "target_name": "被修正对象的名称",
      "reason": "修正原因",
      "new_value": {{}}  // 如果是 modify，提供修正后的值
    }}
  ],
  "is_valid": true/false,
  "summary": "校验总结"
}}
"""

SELF_CORRECTION_USER = """请校验以下抽取结果：

## 已抽取的实体
{entities}

## 已抽取的关系
{relations}
"""
# ============================================================
# 合并抽取 (One-Pass) Prompt
# ============================================================

COMBINED_EXTRACTION_SYSTEM = """你是一个顶级的知识图谱抽取专家。你的任务是从给定文本中严格按照 Schema 抽取实体及实体间的关系。

## 核心准则
1. **Schema 绝对权威**：严禁抽取 Schema 定义之外的任何实体类型或关系类型。
2. **关系约束**：抽取的每条关系，其源实体和目标实体的类型必须符合 Schema 中对应关系类型的 Source/Target 约束。
3. **标准化**：实体名称需简洁。
4. **证据锚定**：每个实体、每条关系都必须给出 `evidence` 字段，填入**原文中能直接支撑该抽取的短句（逐字摘录，不要改写）**。无法在原文找到支撑短句的实体/关系，**不要抽取**。

## 1. 实体类型定义
{entity_types_desc}

## 2. 关系类型定义
{relation_types_desc}

## 输出要求
请严格按照 JSON 格式输出。
{{
  "entities": [
    {{
      "name": "实体名称",
      "entity_type": "实体类型",
      "properties": {{}},
      "evidence": "支撑该实体的原文短句（逐字摘录）",
      "confidence": 0.95
    }}
  ],
  "relations": [
    {{
      "source_name": "源实体名称",
      "target_name": "目标实体名称",
      "relation_type": "关系类型",
      "properties": {{}},
      "evidence": "支撑该关系的原文短句（逐字摘录）",
      "confidence": 0.9
    }}
  ]
}}
"""

COMBINED_EXTRACTION_USER = """请从以下文本中抽取实体和关系：

{text}
"""

# ============================================================
# 归纳抽取 Prompt（inductive 分道，v3）
# ============================================================

INDUCE_FROM_CASES_SYSTEM = """你是知识归纳专家。你的任务不是摘录原文，而是从文本描述的**案例/现象**中，
**归纳概括**出符合目标类型的抽象知识（概念 / 规则 / 模式）。

## 目标归纳类型
{induce_types_desc}

## 核心准则
1. **归纳而非摘录**：产出的是对案例的概括，通常不会逐字出现在原文中。
2. **精简可判别**：每条知识应精简、自洽；若为规则/模式，须尽量给出**可判别的条件**（如量化阈值、可观察行为），避免"存在风险""有异常"这类无法执行的空话。
3. **忠于案例**：只归纳文本确有支撑的知识；无充分案例支撑的，不要臆造。
4. **结构化**：按目标类型的结构模板填写字段。
5. **溯源**：为每条知识给出 `evidence`——支撑该归纳的**原文案例片段摘录**（可较原文精简，但须能追溯到来源）。

## 输出要求
严格 JSON。无可归纳项时返回空数组。
{{
  "items": [
    {{
      "name": "该条知识的精简陈述（作为其名称）",
      "entity_type": "目标归纳类型（必须在上方定义中）",
      "properties": {{ "结构字段名": "值" }},
      "evidence": "支撑该归纳的原文案例片段摘录",
      "confidence": 0.8
    }}
  ]
}}
"""

INDUCE_FROM_CASES_USER = """请从以下文本中归纳出符合目标类型的抽象知识：

{text}
"""

# ============================================================
# 归纳忠实度校验 Prompt（verify_faithfulness，v3）
# ============================================================

FAITHFULNESS_SYSTEM = """你是知识归纳的忠实度审核专家。给定若干「归纳出的知识」及其「源文本」，
判断每条归纳是否**确实被源文本支撑**（而非臆造、过度泛化或与源文本矛盾）。

## 判断标准
- supported=true：源文本中有明确案例/描述支撑这条归纳。
- supported=false：源文本不足以支撑，或归纳过度泛化 / 无中生有 / 与文本矛盾。

## 输出要求
严格 JSON：
{{
  "results": [
    {{ "name": "被审核知识的名称", "supported": true, "reason": "简述依据" }}
  ]
}}
"""

FAITHFULNESS_USER = """## 源文本
{text}

## 待审核的归纳知识
{items}

请逐条判断是否被源文本支撑。
"""

# ============================================================
# 归纳知识语义归并 Prompt（merge_semantic，v3）
# ============================================================

MERGE_SEMANTIC_SYSTEM = """你是知识归纳的语义归并专家。给定同一类型下的若干条「归纳知识」
（它们由不同文档片段独立归纳而来，措辞可能不同），判断哪些条目**表述的是同一条知识**。

## 判断标准
- 同一条知识：触发条件、判别逻辑、指向的行为/概念实质相同，仅措辞或粒度略有差异。
- 不同知识：判别条件不同、适用范围不同、或一条是另一条的特例/泛化（特例与泛化不归并）。

## 输出要求
严格 JSON。groups 中每个数组是一组应当归并的条目 index（至少 2 个）；
representative 为该组中表述最完整、粒度最恰当的一条的 index。不需归并的条目不要出现：
{{
  "groups": [
    {{ "indices": [0, 2], "representative": 0, "reason": "均为同一触发条件的资金异常规则" }}
  ]
}}
"""

MERGE_SEMANTIC_USER = """## 类型：{entity_type}

## 待归并的归纳知识（index: 名称 | 结构字段）
{items}

请判断哪些条目表述同一条知识。
"""
